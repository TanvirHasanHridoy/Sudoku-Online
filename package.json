{
  "name": "sudoku-multiplayer-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev": "npm run dev -w client",
    "server:dev": "npm run dev -w server",
    "build": "npm run build -w client",
    "install:all": "npm install"
  }
}
